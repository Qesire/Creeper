[project]
name = "creeper-scrapy-acquisition"
version = "0.1.0"
description = "Isolated Scrapy acquisition sidecar for Creeper source discovery"
requires-python = ">=3.12"
dependencies = [
    "scrapy==2.18.0",
]

[tool.uv]
package = false
